package com.xifeng.tinkersidea.Weapons;
/*
import com.xifeng.randomtic.Proxy.ClientProxy;
import net.minecraftforge.client.event.ModelRegistryEvent;
import net.minecraftforge.fml.common.eventhandler.SubscribeEvent;
import slimeknights.tconstruct.common.ModelRegisterUtil;
import slimeknights.tconstruct.library.TinkerRegistryClient;
import slimeknights.tconstruct.library.client.ToolBuildGuiInfo;

public class WeaponClientProxy extends ClientProxy {
    public WeaponClientProxy() {};

    public static void init() {
        if (Weapon.glaive != null) {
            ToolBuildGuiInfo info = new ToolBuildGuiInfo(Weapon.glaive);
            info.addSlotPosition(12, 55);
            info.addSlotPosition(42, 27);
            info.addSlotPosition(27, 41);
            TinkerRegistryClient.addToolBuilding(info);
        }

    }
}

 */
